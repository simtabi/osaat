# Build artifacts
/bin/
/dist/
/build/dist/
*.exe
*.test
*.out
coverage.*
*.cover
profile.out

# Go workspace + vendor
/vendor/
go.work
go.work.sum

# Local environment overrides
.env
.env.local
.envrc

# macOS
.DS_Store

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code per-user state
.claude/settings.local.json
.claude/.session/
.claude/cache/

# Tool output — these are user data, never committed (any depth so test
# runs invoked from a subdirectory don't leave stray output behind)
out/
osaat-out/
audit-*/

# Secrets — never committed even if accidentally generated in-repo
secrets.json
*.age
*.tar.age

# Local goreleaser snapshot output
.goreleaser/

# Test fixtures with personally-identifying data
/testdata/local/
